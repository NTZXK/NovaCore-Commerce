{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Executar Arquivo Atual",
            "type": "node", // ou python, cpp, etc.
            "request": "launch",
            "program": "${file}" // <--- Garanta que esta linha não esteja vazia
        }
    ]
}
